# systemd setup
Use a virtualenv on Ubuntu 24.04+ to avoid externally-managed-environment issues.

1. Create user and directories.
2. Create venv in /opt/mc-failover/.venv and install dependencies.
3. Copy service file and adjust paths if needed.
